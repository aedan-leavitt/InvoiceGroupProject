using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.CompilerServices;
using System.Text;
using System.Threading.Tasks;
using groupProject.Common;

namespace groupProject.Items
{
    class clsItemsLogic
    {
        public clsItemsLogic() // Constructor for the clsItemsLogic class
        {
            // Any initialization if needed
        }

        public List<clsItem> getItemsList(List<clsInvoice> invoices)
        {
            //List<clsItem> items = new List<clsItem>(); // Create a list to hold the items
            //foreach (clsInvoice invoice in invoices) // Loop through each invoice
            //{
            //    foreach (clsItem item in invoice.itemList) // Loop through each item in the invoice's item list
            //    {
            //        items.Add(item); // Add the item to the items list
            //    }
            //}
            //return items; // Return the list of items
            return null;
        }
    }
}
